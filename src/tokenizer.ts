export class CSSToken {
    type = ''
    value = ''
    raw = ''
    range = [0, 0]
}

export function tokenizer(src: String = ''): CSSToken[] {
    const tokens: CSSToken[] = []

    return tokens;
}
